{
  "name": "snapshot-publisher",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/src/publish.js",
    "build": "tsc && node --test dist/test/*.test.js",
    "lint": "biome lint --write . --error-on-warnings",
    "lint:check": "biome lint . --error-on-warnings"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.940.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24"
  }
}
